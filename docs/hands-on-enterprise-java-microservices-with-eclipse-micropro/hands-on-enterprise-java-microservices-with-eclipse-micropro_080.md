# 元数据

元数据是 MicroProfile Metrics 中非常重要的一部分。虽然可以公开一个值为 `142` 的指标 `foo`，但它不是自描述的。看到该指标的操作员无法判断这是什么、单位是什么，以及 `142` 是否是一个好值。

元数据用于提供单位以及指标的描述，这样前面的内容现在可以是 `foo: runtime; 142` 秒。这现在允许在显示时正确缩放为*两分二十二秒*。并且收到与此指标相关警报的用户可以理解它指的是某个运行时计时。

