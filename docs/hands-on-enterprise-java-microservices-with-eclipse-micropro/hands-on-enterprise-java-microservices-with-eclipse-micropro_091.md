# 每个指标的标签

标签也可以基于每个指标提供：

```
@Counted(tags=["version=v1","commit=abcde"])
void doSomething() {
  [...]
}
```

此示例在名为 `doSomething` 的指标上定义了两个标签：`version=v1` 和 `commit=abcde`。这些标签将与全局标签合并以用于输出。结合前面的全局标签，输出中将有三个标签。

在本节中，我们了解了如何向指标添加标签以提供额外的元数据。这些标签可以是全局的，适用于服务器公开的所有指标，也可以是特定于应用程序的，适用于单个指标。

