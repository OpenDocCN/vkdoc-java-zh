# 第 8 章

1.  我们在本书和本章中看到了许多示例，表明 MP 配置设置会影响应用程序和 MP 功能。
2.  只要你提供的路径存在，你应该就能看到一次成功的健康检查，并显示有关该路径的信息。
3.  它显示有关 KeyCloak 服务器的信息。如果 KeyCloak 已停止，则会显示错误。
4.  它将不会被找到，因为该指标是在首次请求时延迟生成的。
5.  待定。
6.  对于未受保护的方法，行为应该是类似的。对于受保护的方法，Swagger-UI 调用会失败。
7.  你将看到错误响应。
8.  那是编码后的 MP-JWT。你可以在 curl 命令中将其用作 *Authorization: Bearer ...* 标头值，其中将 ... 替换为在 Access Base64 Token 字段中找到的字符串。

