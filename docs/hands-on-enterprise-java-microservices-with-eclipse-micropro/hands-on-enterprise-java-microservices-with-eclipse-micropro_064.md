# 与云平台集成

大多数云平台同时支持基于 TCP 和 HTTP 的检查。要将健康检查与您选择的云平台集成，您需要配置云部署，使其指向托管您应用程序的节点上的 HTTP 入口点 `/health`。

云平台将在 HTTP 入口点上调用 `GET` 查询；所有已注册的检查都将被执行，各个检查的总和决定了最终结果。

通常，云平台会忽略响应负载，仅查看 HTTP 状态码来确定您应用程序的存活状态或就绪状态。成功的结果 `UP` 将映射为 `200`，而 `DOWN` 将映射为 `503`。



