# 索引

### C

C，转换字符，PatternLayout，195 c，转换字符，PatternLayout，195 categoryPrefixing 属性，184 子日志记录器 ChildLogger.java（列表），24 JDK 1.4 与 log4j 对比，310 父子层次结构及其关系，20，25-26，137-138 类 基于类的配置，JDK 1.4，80-83 编写配置类，253-256 自定义级别类 创建，259-260 实现，259-260 ClientComponent.java（列表），112-116 客户端 实际日志记录中的客户端组件，103，111-116 使用 NDC 区分客户端，308 远程日志记录客户端，101-102 JDK 1.4 的运行，118-120 JMS，222-223 close( ) 方法 JDBCAppender，203 JMSAppender，222 SocketAppender，225 代码。*参见* 列表 Commons 日志记录 API，8-9 复杂 log4j 配置文件（列表），126 CONFIG 级别，13 ConfigDemo.java（列表），56 config.properties（列表），57 配置 最佳实践，308 来自数据库的自定义 log4j 组件，243-258 示例，256-258 通过配置加载器读取信息，245-252 存储日志记录配置的表设计，244 编写配置类，253-256 基于程序配置的缺点，55，73 ErrorHandler，282-283 评估日志记录包及其，7 基于文件的配置，55-58 JDBCAppender 配置，203-204 为其配置文件，205-206 JDK 1.4 日志记录框架配置，73-83 基于类的配置，80-83 基于文件的配置，74-80 JDK 1.4 与 log4j 对比，309，310 JMSAppender，215-217 日志标签库配置文件，287-289 为 Web 服务器配置，289-290 log4j，125-135 基础知识，125-126 默认初始化，127-131 配置的动态加载，131 与 JDK 1.4 对比，309，310 基于程序的配置，130 使用 servlet，132-133 Tomcat 设置，133-135 XML 样式配置，127，308 Logger 对象，log4j，142-143 属性样式配置，308 SocketAppender，225 ConsoleAppender，154 ConsoleHandler 基础知识，37-38 参数，77 构造函数 DailyRollingFileAppender，160 FileAppender，155 FileHandler 创建对象，40 RollingFileAppender，156 在其中关闭配置代码，185 WriterAppender，153 contextPrinting 属性，184 控制，以及日志记录，2-3 转换字符 定义，195 用于 PatternLayout，195-196 成本效益，以及日志记录，4 count 属性 FileHandler 默认属性，40 FileHandler 参数及其，78 customappender.properties（列表），242 CustomerOrder.java（列表），170-171 CustomErrorHandler.java（列表），281-282 customFilter.xml（列表），271 CustomFormatter.java（列表），70 CustomFormatterTest.java（列表），71-72 CustomHandlerDemo.java（列表），92 CustomJDBCAppender.java（列表），208-210，211 customjdbc.properties（列表），210 CustomLevel.java（列表），259-260 CustomLoggerDemo.java（列表），263 CustomLoggerFactory.java（列表），262 CustomLogger.java（列表），261 customlogger.properties（列表），264 customlogger.xml（列表），264-265

