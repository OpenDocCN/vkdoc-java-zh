# 约定

本书中，你将看到多种文本样式，用以区分不同类型的信息。以下是这些样式的示例及其含义说明。

正文中的代码词、数据库表名、文件夹名、文件名、文件扩展名、路径名、虚拟 URL、用户输入以及 Twitter 账号显示如下："我们可以通过使用`include`指令来包含其他上下文。"

代码块示例如下：

```
btn.setOnAction(new EventHandler<ActionEvent>() {
  @Override
  public void handle(ActionEvent event) {
    message.setText("Hello World! JavaFX style :)");
  }
});
```

任何命令行输入或输出均按如下方式书写：

```
$ gradle build
```

**新术语**和**重要词汇**以粗体显示。你在屏幕上（例如菜单或对话框）看到的词语在文中显示如下：点击**安装**开始安装。

### 注意

警告或重要提示会出现在这样的方框中。

### 提示

技巧和窍门会以这种方式呈现。

